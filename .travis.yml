language: node_js
node_js:
  - '10'
after_script:
  - 'cat coverage/lcov.info | ./node_modules/.bin/coveralls'
addons:
  apt:
    packages:
      - oracle-java8-installer
services:
  - postgresql
script:
  - npm run test
